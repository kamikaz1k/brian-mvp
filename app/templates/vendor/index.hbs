<div class="container">
    <h1>Your Items</h1>

    <ul>
        {{#each model.items as |item|}}
            <li>{{item.name}} {{simple-countdown endDateTimeString=item.timerStopAt}}</li>
        {{/each}}
        {{!-- <li>Add Item <button class="btn btn-primary">Set Timer</button></li>
        <li>Add Item <button class="btn btn-primary">Set Timer</button></li>
        <li>Add Item <button class="btn btn-primary">Set Timer</button></li> --}}
        <li>Add Item
            {{#link-to "vendor.item.new" class="btn btn-primary"}}
                Set Timer
            {{/link-to}}
        </li>
    </ul>

    {{#link-to "login" class="btn btn-primary"}}Back{{/link-to}}
</div>
